services:
  kageclip:
    build: .
    image: kageclip:latest
    container_name: kageclip
    ports:
      - "8899:8899"
    volumes:
      - kageclip-downloads:/app/downloads
    restart: unless-stopped

volumes:
  kageclip-downloads:
